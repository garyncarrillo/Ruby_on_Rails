<%= form_with(model: tintere, local: true) do |form| %>
  <% if tintere.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tintere.errors.count, "error") %> prohibited this tintere from being saved:</h2>

      <ul>
      <% tintere.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :idinteres %>
    <%= form.text_field :idinteres, id: :tintere_idinteres %>
  </div>

  <div class="field">
    <%= form.label :valor %>
    <%= form.text_field :valor, id: :tintere_valor %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
