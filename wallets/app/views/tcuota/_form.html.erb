<%= form_with(model: tcuotum, local: true) do |form| %>
  <% if tcuotum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tcuotum.errors.count, "error") %> prohibited this tcuotum from being saved:</h2>

      <ul>
      <% tcuotum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :idcuota %>
    <%= form.text_field :idcuota, id: :tcuotum_idcuota %>
  </div>

  <div class="field">
    <%= form.label :valor %>
    <%= form.text_field :valor, id: :tcuotum_valor %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
